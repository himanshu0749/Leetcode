class Solution {
    public int findSpecialInteger(int[] arr) {
        int quatersize=arr.length/4;
        for(int i=0;i<(arr.length-quatersize);i++){
            if(arr[i]==arr[i+quatersize]){
                return arr[i];
            }
        }
        return -1;
    }
}
